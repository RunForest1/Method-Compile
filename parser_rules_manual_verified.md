# Справочник конструкций для генерации грамматики и ОПС на основе лекций

---

## 0. Устранение левой рекурсии и приведение грамматики к нестрогой форме Грейбах

Преобразование исходной контекстно-свободной грамматики (КС-грамматики) в форму, пригодную для LL(1)-анализа, включает устранение левой рекурсии и факторизацию правил.

### 1. Устранение левой рекурсии

#### Прямая левая рекурсия
Если в грамматике присутствует правило вида:
$A \rightarrow A\gamma$

В этом случае обязательно должно существовать и другое правило вида:
$A \rightarrow \alpha$
где первый символ цепочки $\alpha$ не совпадает с $A$ (иначе нетерминал $A$ — бесполезный).

Такие правила порождают цепочки символов: $\alpha$, $\alpha\gamma$, $\alpha\gamma\gamma$ и т. д. Эти же цепочки можно получить эквивалентными правилами с введением нового нетерминала $A'$:
1. $A \rightarrow \alpha A'$
2. $A' \rightarrow \gamma A' \mid \lambda$

**Пример (на основе арифметических выражений):**
Исходные правила с левой рекурсией:
* $S \rightarrow S + T \mid T$
* $T \rightarrow T * F \mid F$
* $F \rightarrow (S) \mid a$

После устранения левой рекурсии:
* $S \rightarrow T U$
* $U \rightarrow + T U \mid \lambda$
* $T \rightarrow F V$
* $V \rightarrow * F V \mid \lambda$
* $F \rightarrow (S) \mid a$

#### Косвенная левая рекурсия
Косвенная рекурсия возникает, когда имеется совокупность правил вида:
$A \rightarrow B_1\gamma_1, \ B_1 \rightarrow B_2\gamma_2, \ \dots, \ B_n \rightarrow A\gamma_n$

Алгоритм её устранения:
1. Косвенная рекурсия сначала сводится к непосредственной (прямой) путём замены группы правил на правило:
   $A \rightarrow A\gamma_n \dots \gamma_2\gamma_1$
2. При этом учитываются и другие совокупности правил, в которые входят нетерминалы $B_1, \dots, B_n$.
3. После этого прямая левая рекурсия устраняется стандартным способом.

### 2. Приведение к нестрогой нормальной форме Грейбах

Согласно лекциям, **нормальная форма Грейбах** требует, чтобы правые части всех порождающих правил начинались с терминала. В **нестрогой форме Грейбах** допускается, чтобы правая часть правила была пустой ($\lambda$).

#### Алгоритм преобразования (метод подстановки):
В каждом правиле вида $A \rightarrow B\alpha$ нетерминал $B$ заменяется совокупностью правых частей правил $B \rightarrow \gamma_1 \mid \dots \mid \gamma_n$.

В результате получаются правила вида:
$A \rightarrow \gamma_1 \alpha \mid \dots \mid \gamma_n \alpha$

После выполнения всех подобных замен, все правые части правил будут начинаться с терминальных символов или будут пустыми ($\lambda$).

**Пример:**
Берём грамматику с уже устранённой левой рекурсией:
* $S \rightarrow T U$
* $T \rightarrow F V$
* $F \rightarrow (S) \mid a$

**Шаги подстановки:**
1. Нетерминал $T$ в правиле $S \rightarrow T U$ раскрывается через его правые части: $S \rightarrow F V U$.
2. Первый нетерминал $F$ в получившемся правиле $S \rightarrow F V U$ и в правиле $T \rightarrow F V$ заменяется на его правые части $( (S) \mid a )$.

В результате получаем грамматику в нестрогой нормальной форме Грейбах:
* $S \rightarrow ( S ) V U \mid a V U$
* $U \rightarrow + T U \mid \lambda$
* $T \rightarrow ( S ) V \mid a V$
* $V \rightarrow * F V \mid \lambda$
* $F \rightarrow ( S ) \mid a$

---



## 1. Базовая грамматика простых арифметических выражений

### Исходные порождающие правила
* $S \rightarrow S + T \mid T$
* $T \rightarrow T * F \mid F$
* $F \rightarrow ( S ) \mid a$

### Преобразование к нестрогой нормальной форме Грейбах
* $S \rightarrow ( S ) V U \mid a V U$
* $U \rightarrow + T U \mid \lambda$
* $T \rightarrow ( S ) V \mid a V$
* $V \rightarrow * F V \mid \lambda$
* $F \rightarrow ( S ) \mid a$

### Семантические действия генератора ОПС
Генерация ОПС выполняется одновременно с действиями LL(1)-анализатора, для этого наряду с таблицей анализатора необходима таблица генератора, в которой записываются семантические действия по генерации элементов ОПС.

При этом каждой правой части правил порождения соответствует последовательность семантических действий, количество которых **в точности совпадает** с длиной правой части.

В процессе генерации ОПС используется дополнительный магазин, занесение в него и извлечение семантических действий выполняется синхронно с магазином LL(1)-анализатора.

Семантические действия выполняются при их извлечении из дополнительного магазина.

* $\Box$ — пустое действие;
* $a$ — запись в ОПС операнда из входной цепочки (переменной или константы);
* $+$ — запись в ОПС операции бинарного сложения;
* $*$ — запись в ОПС операции бинарного умножения.

### Таблица переходов LL(1)-анализатора и генератора ОПС

| Нетерминал | + | * | ( | ) | a | $\perp$ |
|---|---|---|---|---|---|---|
| **S** | | | $( S ) V U$<br>$\Box\Box\Box\Box\Box$ | | $a V U$<br>$a\Box\Box$ | |
| **U** | $+ T U$<br>$\Box\Box+$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ |
| **T** | | | $( S ) V$<br>$\Box\Box\Box\Box$ | | $a V$<br>$a\Box$ | |
| **V** | $\lambda$ | $* F V$<br>$\Box\Box*$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ |
| **F** | | | $( S )$<br>$\Box\Box\Box$ | | $a$<br>$a$ | |

## 2. Грамматика с дополнительными операциями (+, -, *, /, унарный минус)

### Исходные порождающие правила
* $S \rightarrow S + T \mid S - T \mid T$
* $T \rightarrow T * F \mid T / F \mid F$
* $F \rightarrow ( S ) \mid a \mid + G \mid - GZ$
* $G \rightarrow ( S ) \mid a$
* $Z \rightarrow \lambda$

### Преобразование к нестрогой нормальной форме Грейбах
* $S \rightarrow ( S ) V U \mid a V U \mid + G V U \mid - G V U$
* $U \rightarrow + T U \mid - T U \mid \lambda$
* $T \rightarrow ( S ) V \mid a V \mid + G V \mid - G V$
* $V \rightarrow * F V \mid / F V \mid \lambda$
* $F \rightarrow ( S ) \mid a \mid + G \mid - G Z$
* $G \rightarrow ( S ) \mid a$
* $Z \rightarrow \lambda$

### Семантические действия генератора ОПС
* $\Box$ — пустое действие;
* $a$ — запись операнда в ОПС;
* $+$ — запись в ОПС бинарного сложения;
* $-$ — запись в ОПС бинарного вычитания;
* $*$ — запись в ОПС бинарного умножения;
* $/$ — запись в ОПС бинарного деления;
* $-'$ — запись в ОПС унарного минуса.

### Таблица переходов LL(1)-анализатора и генератора ОПС

| Нетерминал | + | - | * | / | ( | ) | a | $\perp$ |
|---|---|---|---|---|---|---|---|---|
| **S** | $+ G V U$<br>$\Box\Box\Box\Box$ | $- G V U$<br>$\Box\Box-'\Box$ | | | $( S ) V U$<br>$\Box\Box\Box\Box\Box$ | | $a V U$<br>$a\Box\Box$ | |
| **U** | $+ T U$<br>$\Box\Box+$ | $- T U$<br>$\Box\Box-$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ |
| **T** | $+ G V$<br>$\Box\Box\Box$ | $- G V$<br>$\Box\Box-'$ | | | $( S ) V$<br>$\Box\Box\Box\Box$ | | $a V$<br>$a\Box$ | |
| **V** | $\lambda$ | $\lambda$ | $* F V$<br>$\Box\Box*$ | $/ F V$<br>$\Box\Box/$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ |
| **F** | $+ G$<br>$\Box\Box$ | $- G Z$<br>$\Box\Box-'$ | | | $( S )$<br>$\Box\Box\Box$ | | $a$<br>$a$ | |
| **G** | | | | | $( S )$<br>$\Box\Box\Box$ | | $a$<br>$a$ | |
| **Z** | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ |

---
## 3. Грамматика присваиваний и арифметических выражений с индексами массивов

В соответствии с лекциями, оператор присваивания разбирается через начальный нетерминал $A \rightarrow aH := S Z$, где нетерминал $H$ отвечает за выделение индексов массивов.

Здесь $A$ – начальный нетерминал, знаки $+$, $-$, $*$, $/$, $:=$, запятая, скобки круглые, скобки квадратные, переменная $a$, константа $k$ – терминалы:

$$A \rightarrow aH := SZ$$
$$S \rightarrow S + T \mid S - T \mid T$$
$$T \rightarrow T * F \mid T / F \mid F$$
$$F \rightarrow (S) \mid + G \mid - GZ \mid aH \mid k$$
$$G \rightarrow (S) \mid aH \mid k$$
$$H \rightarrow [S] \mid [S, S] \mid \lambda$$
$$Z \rightarrow \lambda$$

### Порождающие правила в нестрогой нормальной форме Грейбах
* $A \rightarrow aH := S Z$
* $S \rightarrow ( S ) V U \mid a H V U \mid k V U \mid + G V U \mid - G V U$
* $U \rightarrow + T U \mid - T U \mid \lambda$
* $T \rightarrow ( S ) V \mid a H V \mid k V \mid + G V \mid - G V$
* $V \rightarrow * F V \mid / F V \mid \lambda$
* $F \rightarrow ( S ) \mid a H \mid k \mid + G \mid - G Z$
* $G \rightarrow ( S ) \mid a H \mid k$
* $H \rightarrow [ S K \mid \lambda$
* $K \rightarrow ] \mid , S ]$
* $Z \rightarrow \lambda$

### Семантические действия
* $\Box$ — пустое действие;
* $a$ — запись в ОПС ссылки на переменную;
* $k$ — запись в ОПС ссылки на константу;
* $+$ — запись в ОПС операции сложения;
* $-$ — запись в ОПС операции вычитания;
* $*$ — запись в ОПС операции умножения;
* $/$ — запись в ОПС операции деления;
* $-'$ — запись в ОПС операции унарного минуса;
* $i$ — операция индексирования одномерного массива;
* $i2$ — операция индексирования двумерного массива;
* $:=$ — запись в ОПС операции присваивания.

### Таблица переходов LL(1)-анализатора и генератора ОПС

| Нетерминал | + | – | * | / | ( | ) | a | k | [ | ] | , | := | ┴ |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **A** | | | | | | | $a H := S Z$<br>$a \Box \Box \Box :=$ | | | | | | |
| **S** | $+ G V U$<br>$\Box \Box \Box \Box$ | $- G V U$<br>$\Box \Box -' \Box$ | | | $( S ) V U$<br>$\Box \Box \Box \Box \Box$ | | $a H V U$<br>$a \Box \Box \Box$ | $k V U$<br>$k \Box \Box$ | | | | | |
| **U** | $+ T U$<br>$\Box \Box +$ | $- T U$<br>$\Box \Box -$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ |
| **T** | $+ G V$<br>$\Box \Box \Box$ | $- G V$<br>$\Box \Box -'$ | | | $( S ) V$<br>$\Box \Box \Box \Box$ | | $a H V$<br>$a \Box \Box$ | $k V$<br>$k \Box$ | | | | | |
| **V** | $\lambda$ | $\lambda$ | $* F V$<br>$\Box \Box *$ | $/ F V$<br>$\Box \Box /$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ |
| **F** | $+ G$<br>$\Box \Box$ | $- G Z$<br>$\Box \Box -'$ | | | $( S )$<br>$\Box \Box \Box$ | | $a H$<br>$a \Box$ | $k$<br>$k$ | | | | | |
| **G** | | | | | $( S )$<br>$\Box \Box \Box$ | | $a H$<br>$a \Box$ | $k$<br>$k$ | | | | | |
| **H** | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $[ S K$<br>$\Box \Box \Box$ | $\lambda$ | $\lambda$ | $\lambda$ | |
| **K** | | | | | | | | | | $]$<br>$i$ | $, S ]$<br>$\Box \Box i2$ | | |
| **Z** | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ |

---

## 4. Грамматика со сравнениями

Служит для сравнения двух выражений операциями $=, \ne, <, >, \le, \ge$.

Порождающие правила для сравнения двух выражений (например, операциями < и >):
* $C \rightarrow S < Z | S>SZ$
  
После преобразования к нормальной форме Грейбах и факторизации:

### Порождающие правила в нестрогой нормальной форме Грейбах
* $C \rightarrow ( S ) V U D \mid a H V U D \mid k V U D \mid + G V U D \mid - G V U D$
* $D \rightarrow = S Z \mid \ne S Z \mid < S Z \mid > S Z \mid \le S Z \mid \ge S Z$
* $Z \rightarrow \lambda$

### Семантические действия
* $=$ — запись в ОПС операции «равно»;
* $\ne$ — запись в ОПС операции «не равно»;
* $<$ — запись в ОПС операции «меньше»;
* $>$ — запись в ОПС операции «больше»;
* $\le$ — запись в ОПС операции «меньше или равно»;
* $\ge$ — запись в ОПС операции «больше или равно».

### Таблица переходов LL(1)-анализатора и генератора ОПС

| Нетерминал | = | $\ne$ | < | > | $\le$ | $\ge$ | ( | a | k | + | - |
|---|---|---|---|---|---|---|---|---|---|---|---|
| **C** | | | | | | | $( S ) V U D$<br>$\Box \Box \Box \Box \Box$ | $a H V U D$<br>$a \Box \Box \Box \Box$ | $k V U D$<br>$k \Box \Box \Box$ | $+ G V U D$<br>$\Box \Box \Box \Box \Box$ | $- G V U D$<br>$\Box \Box -' \Box \Box$ |
| **D** | $= S Z$<br>$\Box \Box =$ | $\ne S Z$<br>$\Box \Box \ne$ | $< S Z$<br>$\Box \Box <$ | $> S Z$<br>$\Box \Box >$ | $\le S Z$<br>$\Box \Box \le$ | $\ge S Z$<br>$\Box \Box \ge$ | | | | | |
| **Z** | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ | $\lambda$ |

---

## 5. Грамматика с условными операторами и циклами

Для управления порядком выполнения используются семантические программы работы с метками перехода. Задействованы счетчик элементов ОПС $k$ и магазин меток.

### Порождающие правила, задающие условные операторы в полной и сокращенной форме (служебные слова if, then, else – терминалы)
* $A \rightarrow \text{if } C \text{ then } A E Z$
* $E \rightarrow \text{else } A \mid \lambda$

Семантические действия генератора ОПС при порождении условных операторов нетерминалом A:
$$\Box \Box 1 \Box \Box 3$$

Семантические действия для нетерминала E:
$$2 \Box$$

Числа 1, 2, 3 в семантических действиях обозначают выполнение семантических программ, генерирующих в ОПС операнды-метки (номера элементов ОПС) и операции условного и безусловного перехода на метки 

### Порождающее правило для задания оператора цикла (служебные слова while, do – терминалы):

* $A \rightarrow \text{while } C \text{ do } A Z$

Семантические действия генератора ОПС при порождении цикла: 

* $4 \Box 1 \Box 5$

Числа 1, 2, 3, 4, 5 в семантических действиях обозначают выполнение семантических программ, генерирующих в ОПС операнды-метки (номера элементов ОПС) и операции условного и безусловного перехода на эти метки.
4


### Семантические программы для управления метками
Семантические программы используют счетчик k – номер очередного генерируемого элемента ОПС , а также еще один магазин – магазин меток.

**Программа 1.**
1. В магазин меток записывается k. 
2. В ОПС записывается пустой элемент – место для будущей метки.
3. В ОПС записывается операция jf – переход при условии false.
   
**Программа 2.**
1. Через верхний элемент магазина меток, как ссылку на ранее заготовленное место для метки, записывается k + 2. 
2. В магазин меток записывается k. 
3. В ОПС записывается пустой элемент – место для будущей метки.
4. В ОПС записывается операция j – безусловный переход. 5
   
**Программа 3.**
1. Через верхний элемент магазина меток, как ссылку на ранее заготовленное место для метки, записывается k.
    
**Программа 4.**
1. В магазин меток записывается k. 

**Программа 5.** 
1. Через верхний элемент магазина меток, как ссылку на ранее заготовленное место для метки, записывается k + 2. 
2. В ОПС записывается метка, значение для которой читается из магазина меток.
3. В ОПС записывается операция j – безусловный переход.

При исполнении ОПС метки, как и другие операнды, записываются в магазин интерпретатора. 

Выполнение операции безусловного перехода j:
1) из магазина извлекается операнд (метка);
2) счетчику, который содержит текущий номер исполняемого элемента ОПС, присваивается значение метки (т.е. производится переход), в магазин ничего не записывается.

Выполнение операции условного перехода jf:
1) из магазина извлекается 1-й операнд (true или false)
2) из магазина извлекается 2-й операнд (метка);
3) если 1-й операнд false, то счетчику, который содержит текущий номер исполняемого элемента ОПС, присваивается значение 2-го операнда (т.е. производится переход), 
если 1-й операнд true, то перехода не производится,
и в обоих случаях в магазин ничего не записывается.

---

## 6. Операторы ввода (read) и вывода (write)

Служат для чтения данных в переменные (включая элементы массивов) и вывода значений выражений или строк.
(служебные слова read, write – терминалы)

### Порождающие правила в нестрогой нормальной форме Грейбах
* $A \rightarrow \text{read} ( a H ) \mid \text{write} ( S )$

Семантические действия генератора ОПС соответственно:

$$\Box \Box a \Box r \mid \Box \Box \Box w$$

### Семантические действия
* $r$ — операция чтения со стандартного устройства ввода в переменную, операнд должен быть ссылкой на переменную
* $w$ — вывод значения арифметического выражения в стандартное устройство вывода, операнд – числовое значение


---

## 7. Дополнительные возможности: Приведение типов, Функции и Строки, вывод, ввод строк
Необходимо будет реализовать.