# 4. Семантические действия для генерации ОПС

Генерация ОПС выполняется синхронно с LL(1)-анализом. При извлечении из магазина семантического действия (нетерминала Z или триггера), выполняется соответствующая программа.

### 4.1. Семантические программы управления метками
Используется магазин меток и счетчик `k` (индекс текущего элемента ОПС).

*   **Программа 1 (P1):** Фиксация условия.
    1. В ОПС записывается `0` (место для метки).
    2. В ОПС записывается операция `jf`.
    3. В магазин меток записывается адрес зарезервированного места (`k-2`).
*   **Программа 2 (P2):** Начало ветки `else`.
    1. Извлекается адрес из магазина меток (ссылка на `jf`).
    2. По этому адресу записывается `k + 2` (переход на начало `else`).
    3. В ОПС записывается `0` (место для метки безусловного перехода `j`).
    4. В ОПС записывается операция `j`.
    5. В магазин меток записывается новый адрес зарезервированного места (`k-2`).
*   **Программа 3 (P3):** Конец `if`.
    1. Извлекается адрес из магазина меток.
    2. По этому адресу записывается текущий `k` (адрес следующего за `if` оператора).
*   **Программа 4 (P4):** Начало цикла `while`.
    1. В магазин меток записывается текущий `k` (адрес начала проверки условия).
*   **Программа 5 (P5):** Конец цикла `while`.
    1. Извлекается адрес `jf` (из P1) и заполняется значением `k + 2`.
    2. Извлекается адрес начала условия (из P4) и записывается в ОПС как операнд-метка.
    3. В ОПС записывается операция `j`.

---

# 5. Список операций ОПС

| Операция | Описание |
| :--- | :--- |
| `+`, `-`, `*`, `/` | Арифметические бинарные операции |
| `-'` (NEG) | Унарный минус (инверсия знака) |
| `=`, `<>`, `<`, `>`, `<=`, `>=` | Операции сравнения |
| `:=` | Присваивание (стек: `адрес`, `значение`) |
| `j` | Безусловный переход (стек: `метка`) |
| `jf` | Переход по False (стек: `метка`, `флаг`) |
| `r` (READ) | Ввод в переменную (стек: `адрес`) |
| `w` (WRITE) | Вывод значения (стек: `значение`) |
| `i` (INDEX1) | Индексация одномерного массива |
| `i2` (INDEX2) | Индексация двумерного массива |

---

# 6. Формат ОПС

ОПС — массив структур `RpnElement`:
1. **VAR:** Ссылка на таблицу символов (идентификатор).
2. **CONST:** Значение константы.
3. **OP:** Код операции (п. 5).
4. **LABEL:** Целое число (адрес в массиве ОПС).

---

# 7. Таблица переходов LL(1)-анализатора и генератора ОПС

| Нетерминал | L_ID | L_INT / L_STR | := | + / - | * / / | CMP | if | then | else | while | do | read | write | ( | ) | [ | ] | , | ; / ┴ |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **Prog** | `Stat Prog`<br>$\Box \Box$ | | | | | | `Stat Prog`<br>$\Box \Box$ | | | `Stat Prog`<br>$\Box \Box$ | | `Stat Prog`<br>$\Box \Box$ | `Stat Prog`<br>$\Box \Box$ | | | | | | $\lambda$<br>$\Box$ |
| **Stat** | `Assign`<br>$\Box$ | | | | | | `IfStat`<br>$\Box$ | | | `WhileStat`<br>$\Box$ | | `Read`<br>$\Box$ | `Write`<br>$\Box$ | | | | | | |
| **Assign** | `ID Idx := Expr Z ;`<br>$a \Box \Box \Box := \Box$ | | | | | | | | | | | | | | | | | | |
| **IfStat** | | | | | | | `if Cond then Stat Else Z`<br>$\Box \Box 1 \Box \Box 3$ | | | | | | | | | | | | |
| **Else** | | | | | | | | | `else Stat`<br>$\Box 2$ | | | | | | | | | | $\lambda$<br>$\Box$ |
| **While** | | | | | | | | | | `while Cond do Stat Z`<br>$4 \Box 1 \Box 5$ | | | | | | | | | |
| **Cond** | `Expr CMP Expr Z`<br>$\Box \Box \Box op$ | `Expr CMP Expr Z`<br>$\Box \Box \Box op$ | | `Expr CMP Expr Z`<br>$\Box \Box \Box op$ | | | | | | | | | | `Expr CMP Expr Z`<br>$\Box \Box \Box op$ | | | | | |
| **Expr** | `Term ETail`<br>$\Box \Box$ | `Term ETail`<br>$\Box \Box$ | | `Term ETail`<br>$\Box \Box$ | | | | | | | | | | `Term ETail`<br>$\Box \Box$ | | | | | |
| **ETail** | | | | `op Term ETail`<br>$\Box \Box op$ | | $\lambda$<br>$\Box$ | | | | | | | | | $\lambda$<br>$\Box$ | | | | $\lambda$<br>$\Box$ |
| **Term** | `Fact TTail`<br>$\Box \Box$ | `Fact TTail`<br>$\Box \Box$ | | `Fact TTail`<br>$\Box \Box$ | | | | | | | | | | `Fact TTail`<br>$\Box \Box$ | | | | | |
| **TTail** | | | | $\lambda$<br>$\Box$ | `op Fact TTail`<br>$\Box \Box op$ | $\lambda$<br>$\Box$ | | | | | | | | | $\lambda$<br>$\Box$ | | | | $\lambda$<br>$\Box$ |
| **Fact** | `ID Idx`<br>$a \Box$ | `Const`<br>$k$ | | `+ UOp`<br>$\Box \Box$ / `- UOp Z`<br>$\Box \Box -'$ | | | | | | | | | | `( Expr )`<br>$\Box \Box \Box$ | | | | | |
| **UOp** | `ID Idx`<br>$a \Box$ | `Const`<br>$k$ | | | | | | | | | | | | `( Expr )`<br>$\Box \Box \Box$ | | | | | |
| **Idx** | $\lambda$<br>$\Box$ | | $\lambda$<br>$\Box$ | $\lambda$<br>$\Box$ | $\lambda$<br>$\Box$ | $\lambda$<br>$\Box$ | | | | | | | | | | `[ Expr ITail`<br>$\Box \Box \Box$ | | | |
| **ITail** | | | | | | | | | | | | | | | | | `] Z`<br>$\Box i$ | `, Expr ] Z`<br>$\Box \Box \Box i2$ | |
| **Read** | | | | | | | | | | | | `read ( ID Idx ) ;`<br>$\Box \Box a \Box \Box r \Box$ | | | | | | | |
| **Write** | | | | | | | | | | | | | `write ( Expr ) ;`<br>$\Box \Box \Box \Box w \Box$ | | | | | | |
